#include "main.h"

/**
 * _isupper - chech if c is upper
 *
 * 0c: input for alphabet
 *
 * return: 1 if success uppercase,0 ifnot
*/

int _isupper(int c);
{
	if (c >= 65 && c <=90)
		return (1);
	else
		return (0);
}
